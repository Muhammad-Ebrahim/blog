<div class="service-container">
    <p class="service-container__header">
        I'm working on packaging the services to fit your needs so You could get the most out of our cooperation. <br>
        Services should be ready at 1/3/2018.
        <img class="service-container__arm" src="/strong.svg" alt="arm">
    </p>
</div>
 <% @services.each do |service| %>
    <%= service.title %>
    <br>
    <%= sanitize service.content %>
    <br>
    <%= link_to "READ MORE", service_path(service) %>
    <br>
        <%= link_to "Edit", edit_service_path(service) %>    
 <% end %>